**LiteAdmin - UI 原型图（Markdown 版）**

### 1. 整体布局原型（主界面）

```markdown
┌──────────────────────────────────────────────────────────────────────────────────────┐
│  LiteAdmin - demo.db  [● Connected]                          ◀ □ ✕               │  ← AppHeader
├──────────────────────────────────────────────────────────────────────────────────────┤
│  文件  编辑  查看  工具  帮助                  新建查询  刷新  执行  格式化  导出   │  ← 工具栏
├───────────────┬──────────────────────────────────────────────────────┬───────────────┤
│               │                                                      │               │
│   数据库浏览器 │                   主工作区 (Tabs)                    │  属性面板     │
│   (Sidebar)   │                                                      │  (可选)       │
│               │  ┌──────────┬──────────┬──────────┬──────────┐      │               │
│  ● demo.db    │  │ SQL查询  │  users数据 │  表结构   │  ...     │      │  表信息       │
│  ├ Tables     │  └──────────┴──────────┴──────────┴──────────┘      │  • 字段数: 8  │
│  │  ├─ users  │                                                      │  • 行数: 1248 │
│  │  ├─ orders│  ← 当前激活 Tab: SQL查询                             │  • 大小: 2.4MB│
│  │  └─ logs  │                                                      │               │
│  ├ Views      │  SELECT * FROM users WHERE age > 18;                 │               │
│  ├ Indexes    │  ──────────────────────────────────────────────────   │               │
│  └ Triggers   │  [ 执行 ] [ 格式化 ] [ 保存 ]                        │               │
│               │                                                      │               │
│               │  ┌────────────────────────────────────────────────┐   │               │
│               │  │  id  | name      | age | email               |   │   │               │
│               │  │  1   | 张三      | 28  | zhang@example.com   │   │   │               │
│               │  │  2   | 李四      | 35  | li@example.com      │   │   │               │
│               │  │  ...                                            │   │   │               │
│               │  └────────────────────────────────────────────────┘   │               │
│               │                                                      │               │
├───────────────┴──────────────────────────────────────────────────────┴───────────────┤
│  1248 行 | 执行时间: 45ms | SQLite 3.46.0 | demo.db (4.8 MB)     暗黑模式  中文    │  ← StatusBar
└──────────────────────────────────────────────────────────────────────────────────────┘
```

---

### 2. 左侧边栏 - 数据库浏览器（展开状态）

```markdown
数据库浏览器
────────────────────────────────
● demo.db                    [已连接]
├─ Tables (12)
│   ├─ users                  (1248 行)
│   ├─ orders                 (3421 行)
│   ├─ products
│   └─ logs
├─ Views (3)
│   └─ active_users
├─ Indexes (8)
├─ Triggers (2)
└─ Schemas

最近打开
• test.db
• production.db
```

---

### 3. SQL 编辑器 Tab 详细原型

```markdown
[ SQL查询 ] [ users数据 ] [ 表结构 ] 

┌──────────────────────────────────────────────────────────────────────┐
│  SELECT * FROM users                                                 │
│  WHERE status = 'active'                                             │
│  ORDER BY created_at DESC                                            │
│  LIMIT 1000;                                                         │
│                                                                      │
│  ────────────────────────────────────────────────────────────────    │
│  [ 执行选中语句 ] [ 执行全部 ] [ 解释执行计划 ] [ 格式化 ]           │
└──────────────────────────────────────────────────────────────────────┘

查询结果 (共 248 行，返回前 100 行)     执行时间: 32ms

┌───────┬────────────┬─────┬──────────────────────────────┐
│  ID   │  用户名    │ 年龄│  邮箱                        │
├───────┼────────────┼─────┼──────────────────────────────┤
│  1    │  张三      │ 28  │ zhangsan@example.com         │
│  2    │  李四      │ 35  │ lisi@demo.com                │
│  ...  │  ...       │ ... │  ...                         │
└───────┴────────────┴─────┴──────────────────────────────┘

[ 上一页 ]  1 / 3  [ 下一页 ]   每页: 100 条
```

---

### 4. 数据表格 Tab 原型（Table Data）

```markdown
users 表 - 数据浏览                          [ 刷新 ] [ 新增 ] [ 删除选中 ] [ 导出 ]

筛选:  [ 姓名 ▾ ] [ 年龄 > ___ ] [ 状态 = 活跃 ]     共 1248 条记录

┌───────┬────────────┬─────┬──────────────────────┬────────────┬──────────┐
│  ID   │  用户名    │ 年龄│  邮箱                │  注册时间  │  状态    │
├───────┼────────────┼─────┼──────────────────────┼────────────┼──────────┤
│▶  42  │  王五      │ 29  │ wangwu@demo.com      │ 2025-12-01 │  活跃    │
│   43  │  赵六      │ 31  │ zhaoliu@gmail.com    │ 2025-11-15 │  活跃    │
│   44  │  孙七      │ 25  │ sunqi@163.com        │ 2025-10-20 │  禁用    │
└───────┴────────────┴─────┴──────────────────────┴────────────┴──────────┘

右键菜单示例：
  复制单元格 | 复制整行 | 编辑行 | 删除行 | 查看 JSON
```

---

### 5. 表结构 Tab 原型

```markdown
users 表结构

字段列表
┌───┬──────────┬──────────┬────────┬──────┬────────┬─────────────┐
│ # │ 字段名   │ 类型     │ 长度   │ NULL │ 默认值 │ 备注        │
├───┼──────────┼──────────┼────────┼──────┼────────┼─────────────┤
│ 1 │ id       │ INTEGER  │        │ NO   │        │ 主键 自增   │
│ 2 │ username │ TEXT     │        │ NO   │        │ 唯一        │
│ 3 │ age      │ INTEGER  │        │ YES  │ NULL   │             │
│ 4 │ email    │ TEXT     │        │ YES  │        │             │
└───┴──────────┴──────────┴────────┴──────┴────────┴─────────────┘

索引列表
• idx_users_username (username)
• idx_users_age_email (age, email)

DDL 预览
CREATE TABLE users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  username TEXT NOT NULL UNIQUE,
  ...
);
```

---

### 6. 暗黑模式视觉（文字模拟）

整体背景为深色，代码区域为 `#1e1e1e`，表格隔行 `#252526`，文字 `#cccccc`。

**需要我继续提供以下哪个原型的更详细版本？**

- 欢迎页 / 新建数据库弹窗
- 导入导出对话框
- 设置页面
- ER 图预览界面
- 移动端适配版本（响应式）

或者告诉我您想重点突出哪个界面，我可以进一步细化。