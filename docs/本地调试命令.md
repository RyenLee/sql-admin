# 1. 打开第一个终端 - 启动后端

cd d:\Dev\workspace\2026\05\Rust\sql-admin
cargo run --package sql-admin-backend

# 2. 打开第二个终端 - 构建前端 CSS + 启动前端

cd d:\Dev\workspace\2026\05\Rust\sql-admin\crates\frontend
npm run build:css
trunk serve --port 8080

# 注意：每次修改 Tailwind 样式类后，需重新执行 npm run build:css

# 开发时可另开终端运行 CSS 监听：npm run watch:css

# 3. 在浏览器中访问

# http://localhost:8080
