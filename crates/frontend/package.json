{
  "name": "sql-admin-frontend",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./styles.css -o ./public/styles.css --minify",
    "watch:css": "npx @tailwindcss/cli -i ./styles.css -o ./public/styles.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.15",
    "tailwindcss": "^4.3.0"
  }
}
